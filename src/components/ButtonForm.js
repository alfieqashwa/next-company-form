export const ButtonForm = ({ type }) => (
  <button
    className='w-full py-2 mt-8 text-lg text-center text-gray-100 transition duration-200 ease-in-out bg-gray-500 rounded-lg hover:bg-gray-400 hover:text-gray-600 focus:outline-none'
    type={type}>
    Create
  </button>
)